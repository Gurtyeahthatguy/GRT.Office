{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the spreadsheet. Network and shell access are denied: neither tauri-plugin-http nor tauri-plugin-shell is compiled in, so those permissions have nothing to attach to. A spreadsheet has no reason to reach a network, and the one thing that historically made spreadsheets dangerous — a scripting engine — is not compiled in either. Window creation stays denied to the frontend.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-ask",
    "core:webview:deny-create-webview",
    "core:webview:deny-create-webview-window",
    "core:window:deny-create"
  ]
}
