{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities. Network and shell access are denied: see the deny entries below, and note that neither tauri-plugin-http nor tauri-plugin-shell is compiled into the binary at all, so those permissions have nothing to attach to.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-ask",
    "core:webview:deny-create-webview",
    "core:webview:deny-create-webview-window",
    "core:window:deny-create"
  ]
}
